# 2026-08-27 (후반) — 문항당 1콜 전 병렬 완성 + 모델 세대 교체

커밋 3개(d1f4a1b · 0fb7a87 · d7ea390)의 묶음 보고. 오전의 "출력 범위 분할" 전환을
논리적 종점까지 밀었다: **모든 문서 파싱·해설 생성이 "예열 1콜 → 문항당 1콜 전 병렬 →
병합 저장" 하나의 프로세스**가 됐다.

```
[예열: 기출=0토큰 / 주차=번호 발견] → [문항당 1콜, sonnet-5, 전 병렬] → [번호 병합 → DB]
                                        └ 필터 결손 = 정확히 그 문항 하나 (skipped_questions 보고)
```

## 1. 모델 중앙화 + 세대 교체 (d1f4a1b)

- `MODELS { parse: sonnet-5, explanation: opus-5, light: haiku }` — 모델 문자열 중앙화.
- 파싱 sonnet-4-6 → **sonnet-5** (단가 33%↓ + 신세대). 서술형 채점도 같이 올라감(실사용 관찰 중).
- **필터 이중판별**: 신세대는 필터 거절이 에러가 아닌 정상응답(`stop_reason=refusal`) —
  `callClaudeTextDetailed` 가 감지해 기존 문자열 판별(`isContentFilterError`)로 통일.
- **temperature 가드**: 신세대는 샘플링 파라미터 제거(전달 시 400) — 중앙에서 조용히 떨굼.
  채점의 temperature 0(결정성)이 무효화된 점 유의.
- pages 청킹 데드코드 일괄 삭제 (엔진 pages 정책·경계 정렬·재배정·지문 전파 + 테스트).

## 2. 기출 해설 vision 통일 (0fb7a87)

- 평가원형 기본이던 **정규식 텍스트 파서 삭제** (EBS 폰트 인코딩 PDF 에서 조용히 깨져
  별도 vision 토글이 필요했음) — 학평/평가원 판별은 `exam_month` 로 프롬프트만 선택.
- `upload-explanation-vision`·`debug-explanation` 라우트, bulk 다이얼로그 Vision 토글 삭제.
- 평가원 해설 업로드가 무료(정규식)→유료(~$1)가 된 대신, 조용한 실패 모드가 사라짐.

## 3. 추출+AI 보완 한 콜 통합 + 문항당 1콜 (d7ea390)

- 해설 프롬프트에 **필드별 규칙 분리**: intent·translation = 원문 그대로(창작 금지, 해석
  원문 보장), solution·vocabulary = 원문 기반 작성. 평가원형은 "원문 풀이 통합·확장" —
  기존 체인이 원문 풀이를 AI 풀이로 덮어쓰던 문제도 해결.
- **중복 생성 사슬 삭제**: `generate-explanation` 라우트 + `generateExplanations`(-199줄),
  `explanations-drain` + `reading-explanations.ts` + 복구 칩. 주차 파싱 프롬프트에 이미
  보강 작성 규칙이 있어 드레인은 같은 일을 두 번 하던 것. 어휘 병합·단어 DB 보강은
  upload-explanation 라우트로 이식.
- **`NUMBERS_PER_RANGE_CALL = 1`** (ranged.ts, 유일한 병렬 폭 손잡이): 기출 28병렬 ·
  주차 ~20병렬. 단일 사용자·단일 작업 전제(rate limit 한 자릿수 %).
- **필터 문항 단위 격리** (`rangedParseCallIsolated`, 3파이프라인 공용): 그룹이 필터에
  걸리면 문항 단위 재시도(캐시 히트) — 진짜 걸린 문항만 결손, 번호로 보고 → UI 토스트.
- 해설 파싱 opus → sonnet-5 (캐시 읽기·출력 단가 40%, 시험당 ~$2.8→$1.1).
  opus 는 변형 분석 리포트(text-compare) 전용으로 잔존.

## 실측 (2026-08-27, 실제 파일 4종)

| | 기출 문제 8p | 기출 해설 평가원형 31p | 주차 13p | 기출 해설 학평형 6p |
|---|---|---|---|---|
| 시간 | 51.8s | 46.4s (통합 전 144s) | 45.4s | 41s |
| 비용 | $0.33 | $1.13 | ~$0.5 | $0.78 |
| 완전성 | 27 (25번 도표 제외 정상) | 28/28 + 풀이·어휘 100% | 27항목 (소문항 분리 정확) | 28/28 |
| 해석 원문 겹침율 | — | **0.92** (저점 0건) | — | **0.96** (저점 0건) |
| 필터 결손 | 0 | 0 | 0 | 0 |

문서 크기와 무관하게 전부 40~50초 — 벽시계 = 예열 + 가장 무거운 문항 1개.
41~45 장문 공유 지문은 콜마다 문서 전체(캐시)를 보므로 온전 (41=42 지문 1,225자 동일 실측).

## 부작용 가능 지점

- **기능 축소 (의도됨)**: 기출 "AI 해설 생성" 버튼·주차 해설 복구 칩 삭제 — 재생성하려면
  해설 PDF 재업로드. 수동 추가 문항의 빈 해설 자동 보충 없음(편집기 직접 입력).
- 서술형 채점이 sonnet-5 + temperature 지정 불가로 바뀜 — 판정 일관성 관찰 필요.
- sonnet-5 풀이 품질(opus 대비)은 길이상 건강(474~580자)하나 내용 검수는 실사용 몫.
- 병렬 폭 전제: 주차·기출을 동시에 올리면 순간 50콜 — 단일 사용자면 무관, 다중 사용자
  전환 시 `NUMBERS_PER_RANGE_CALL` 재검토.

## 수동 확인 포인트

- 기출 해설 업로드 1회: 풀이 품질 눈검수 (아쉬우면 parseExplanationsRanged 의
  `MODELS.parse` → `MODELS.explanation` 한 줄 복귀).
- 서술형 채점 몇 건: 판정이 기존 기준과 일관된지.
